# =====================================================================
# Lighthouse — DEV & CI tooling (NOT runtime dependencies)
# ---------------------------------------------------------------------
# rank.py imports ONLY numpy / pandas / pyyaml (see requirements.txt).
# These tools run in CI and local dev. Keeping them out of requirements.txt
# preserves the minimal rank-time footprint (Efficiency / Security).
# =====================================================================
pytest==8.3.4
pytest-cov==6.0.0
ruff==0.8.4
black==24.10.0
mypy==1.13.0

# type stubs so mypy can check pandas / yaml usage without bundling runtime libs
pandas-stubs==2.2.3.241126
types-PyYAML==6.0.12.20240917

# sandbox-server smoke test (FastAPI TestClient). App-only — not a rank-time dep.
fastapi==0.115.6
httpx==0.28.1
